# Clé API Gemini — utilisée UNIQUEMENT côté serveur, jamais exposée au navigateur.
GEMINI_API_KEY=your-gemini-api-key

# Optionnel : "gemini-2.5-flash" (rapide, par défaut) ou "gemini-2.5-pro" (plus puissant, plus lent)
GEMINI_MODEL=gemini-2.5-flash

NEXT_PUBLIC_APP_URL=http://localhost:3000
